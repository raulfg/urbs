{
  "name": "urbs",
  "version": "0.0.0",
  "private": true,
  "description": "Motor de sandbox urbano generado a partir de datos geograficos abiertos",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "test": "node --test packages/*/test/*.test.js"
  }
}
